# muscle_training

SQL/Python の短時間ドリル（日次）・実務相当演習（週次）の提出物を管理するリポジトリ。課題文・自分の回答・レビューをタスクごとに残している。

## 学習の目的と中心的な課題

Snowflake/dbt を中心とした Analytics/Data Platform 領域の技術習得。

**中心的な課題は「答えを見れば理解できるが、白紙では書けない」という、認識(recognition)と再生(production)のギャップ。** ここにある解答は、そのギャップを埋めるための練習の記録である。

したがってこのリポジトリでの作業は、**「動くコードが書けたか」より「自力で組み立てられたか」を重視する。**

## スキルの使い分け

| 依頼された内容 | 読むファイル |
|---|---|
| SQL の日次ドリルを出題 | `agent/sql_daily/SKILL.md` |
| SQL の週次演習を出題 | `agent/sql_weekly/SKILL.md` |
| Python の日次ドリルを出題 | `agent/python_daily/SKILL.md` |
| Python の週次演習を出題 | `agent/python_weekly/SKILL.md` |
| 提出した解答のレビュー（**1本目**） | `agent/review_1_correctness/SKILL.md` |
| 提出した解答のレビュー（**2本目**） | `agent/review_2_quality/SKILL.md` |

**出題とレビューは分離している。** 出題スキルは問題文を作って待つところまで、レビュースキルは提出物を評価するところだけを担当する。

### レビューは2パスに分かれている

1本の長い手順を全部こなすと項目の取りこぼしが出るため、意図的に分割している。

#### どちらを実行するかは、言葉ではなく状態で判断する

「レビューして」と言われただけでは、Pass 1 なのか Pass 2 なのか決まらない。**必ず対象タスクの状態を確認してから決める。**

```bash
# 対象タスクフォルダの既存レビューを確認する
ls {タスクフォルダ}/
grep -l "進捗：Pass 1" {タスクフォルダ}/FB_from_AI_*/README.md 2>/dev/null
```

| 状態 | 実行するもの |
|---|---|
| `FB_from_AI_*/README.md` が**無い** | **Pass 1** |
| あり、かつ `> 進捗：Pass 1（正確性）まで完了` を含む | **Pass 2** |
| あり、かつ `> 進捗：Pass 1・Pass 2 完了` を含む | **どちらも実行しない。** 何をしたいかユーザに確認する（再レビューなら新しい連番で Pass 1 から） |

**ユーザが「Pass 2 をやって」と明示した場合は、その指示に従う。** ただし Pass 1 の出力が見つからなければ、その旨を伝えてから Pass 1 を提案する。

| | 担当 | 出力 |
|---|---|---|
| Pass 1 | 課題の主題・褒める・制約の充足・正確性・本番運用の必須トレース・修正版スクリプト | `FB_from_AI_{NN}/README.md` を新規作成 |
| Pass 2 | イディオム・パフォーマンス・命名・確認質問・まとめ・英語 | 上記に追記 + `FB_ENG_{NN}/README.md` |

- **指示は2回に分けて出す。** 1回の指示で両方を続けて実行しない
- **Pass 2 は Pass 1 とは別のセッションで実行する。** 同じセッションだと Pass 1 の思考が残っているため、Pass 2 による Pass 1 の点検が「自分の結論に同意するだけ」になって機能しない
- **Pass 1 は PR を取得しない**（英語は Pass 2 の担当）
- **Pass 2 は Pass 1 の出力をファイルから読んでから始める**（重複回避と、Pass 1 の抜けの点検）
- Pass 1 が終わったら、ユーザーに Pass 2 に進むか確認する
- **両方通すのが既定。** 片方だけで終わらせない

## フォルダマップ

```
sql/
  _daily/task_d_{NN}/        課題文(README.md)・環境構築(setup.sql)・自分の回答
  _weekly/task_w_{NN}/       同上（dbt プロジェクトを含む）
  envs/envs/.env.{環境名}     Snowflake接続情報（追跡対象外）
  run_sql.py                  SQL実行ランナー（相対パスを受け取る）
python/
  _daily/task_d_{NN}/        課題文(README.md)・自分の回答
  _weekly/task_w_{NN}/       同上
agent/                        スキル定義（上記の表）
```

各タスクフォルダの構成：

| パス | 内容 |
|---|---|
| `README.md` | 課題文 |
| `setup.sql` | 環境構築SQL（SQLタスクのみ） |
| `my_answer.sql` / `main.py` 等 | **自分の回答**（無援で書いた記録） |
| `FB_ENG_{連番}/README.md` | PR本文（オリジナル）とそれに対する英語レビュー・添削 |
| `PR_FROM_ME.md` | 自分が書いたPR本文（過去のタスクに残存・ghのフォールバック用） |
| `FB_from_AI_{NN}/` | AIによる技術的なレビュー |

### 過去のタスクは命名が違う

2026年9月上旬までのタスクは `task{NN}` や `{YYYY-MM-DD}` 形式で、以下の不規則がある。

- `FB/` `answers/` `answer/` — 別のAIが生成したフィードバックと模範解答（現在は生成しない）
- `FB_from_Claude_{NN}/` — 別ツールで生成したレビュー

**タスクを列挙するときは `ls sql/_daily/` のようにディレクトリ全体を見る。** グロブでは取りこぼす。

## 絶対のルール

### コードを実行しない

`python`、`uv run`、`dbt run`、`run_sql.py` を実行しない。**Snowflake への接続はクレジットを消費するため特に禁止。**

コードの挙動を述べるときは**机上トレースで導き、実行していないことを明記する。** 「実行して確認した」と読める書き方をしない。集計値・呼び出し回数・ループ回数を述べる場合は、コードを追って得た結果であることを示す。

（Python のドリルはローカル実行にコストがかからないため、許可すれば検証は強くなる。許可する場合は「SQL/dbt/Snowflake は実行しない。Python は実行してよいが、実測値と机上トレースを区別して書く」に読み替える。既定は実行しない。）

### 自分の回答を書き換えない

`各タスクフォルダ` 直下のスクリプトは**無援で書いた記録**なので、修正しない。ここを直すと「そのとき何を書けたか」の記録が失われ、後から成長を測れなくなる。

修正版を示すときは、コード片として文中に書くか、`FB_from_AI_{NN}/` 配下に別ファイルとして置く。

### 模範解答を作らない

出題時も、レビュー時も、**模範解答ファイルを置かない。** 答えを1つ提示するより、自分の解答を複数の観点で検証するほうが学習効果が高い。過去に生成された模範解答は、コードが破損していたりバグを含んでいたりした（下記）。

## タスクを扱うときの前提

- **複数のタスクが存在するので、対象を勝手に決めない。** どのタスクの話か確認する
- **PR も練習対象**（英語を含む）。`gh pr list --state all` で確認できる

## 繰り返し出ている弱点

出題・レビューのどちらでも、以下は**未消化のテーマとして扱う。** 3タスク以上にまたがって出ているものだけを挙げている。

1. **検証の設計** — 実装と同じ操作で期待値を作ると、検証は実装のコピーになり何も守らない（2回発生）。判定法は「**その検証が失敗する入力を1つ挙げられるか**」
2. **決定論性** — 同点時の挙動、非安定ソート（`sort_values` の既定 `kind='quicksort'` は非安定）、文字列日付のソート、index の非連続。「同じ入力で同じ出力が返るか」の観点が抜けやすい
3. **`assert` と `raise` の使い分け** — プログラマのバグは `assert`、データの異常は `raise`（`assert` は `python -O` で削除される）
4. **捨てられる `.copy()`** — 判定基準は「その変数を書き換えるか」だけ。書き換えるなら必要、読むだけなら不要
5. **境界値** — 半開区間 `[start, end)` になっているか、境界の時刻成分を正規化しているか、0件入力で何が返るか
6. **綴り** — コミットメッセージ・識別子・パスの綴りは grep 可能性を左右する（`claense`、`monday`、`taks03` の実例）
7. **Conventional Commits のコロン抜け** — `feat(scope) msg` ではなく `feat(scope): msg`（3回発生）

## 過去の模範解答の信頼度（`FB/` があるタスクのみ）

過去のタスクの `FB/` や `answers/` にある模範解答は別のAIが生成したもので、実測で以下の欠陥が確認されている。**正解として扱わない。**

- コードブロックの配列リテラルが欠落し、模範解答が `SyntaxError` になる（複数箇所）
- 模範解答自体にバグ（`except Exception` + `print` で終了コード0を返し、全リトライ失敗後に「成功」と報告するサイレント障害）
- 修正指示が別のバグを生む（境界に `DATE_TRUNC` が無い状態で「`>=` と `<` に直せ」とだけ指示しており、従うとデータ欠損が発生する）
- 断定が過剰（「スカラサブクエリなのでプルーニングが100%効く」）
- 逆の助言（データ品質チェックに `assert` を推奨）

**新しいタスクには `FB/` は存在しない。**
