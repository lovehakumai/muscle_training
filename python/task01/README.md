**すまない、私のミスだ！** 日次の短時間ドリル（15〜20分）としては、出題のスコープが広すぎた上に、君に余計な準備の負担を強いてしまった。可視化ライブラリの導入や実際のAPI通信を伴う構成は、週次（Weekly）でじっくり取り組むべきテーマだった。

君の指摘通り、テスト用のダミー関数やデータは私の方で提供するのが筋だ。

デイリードリル本来の規約に則り、**「Pandas単体、または軽量なダミー関数で完結する、単一テーマ・小規模な問題」**として、問題をシンプルに作り直した。今回は**「リトライロジックの基礎（通信なし）」**に絞って出題する。

---

# 課題：README.md

今回選んだテーマ: **4. リトライロジックの基礎（通信なし）**

### シナリオ:
本番のデータパイプラインでは、APIサーバーの一時的な過負荷（503 Service Unavailableなど）により、一時的にデータ抽出に失敗することが頻繁に発生します。数回失敗しても自動で再試行し、最終的にデータを安全に取得できるレジリエントな抽出処理を、外部通信を行わずにテスト実装します。

### 制約条件:
- `tenacity` ライブラリを用いてリトライロジックを実装すること。
- 4回目の呼び出しで成功するよう設計された、以下のダミー関数を使用すること。

### 準備するもの:
以下のコードをコピーして、君のスクリプトのベースにしてくれ。

```python
import pandas as pd
from tenacity import retry, stop_after_attempt, wait_fixed # 必要なモジュールは適宜調整してくれ

# ----------------------------------------------------
# 準備用のダミー関数（モックAPI）
# 3回目までの呼び出しは一時的なエラーを投げ、4回目でデータ（DataFrame）を返す。
# ----------------------------------------------------
call_count = 0

def fetch_data_from_unstable_api():
    global call_count
    call_count += 1
    if call_count < 4:
        print(f"[API] Call {call_count}: 503 Service Unavailable (Temporary Error)")
        raise RuntimeError("API Temporary Error")
    
    print(f"[API] Call {call_count}: 200 OK (Success)")
    return pd.DataFrame({
        "campaign_id": [101, 102],
        "clicks": [1500, 2300]
    })
```

### 期待するスクリプトの条件:
- 上記の `fetch_data_from_unstable_api` を呼び出し、一時的な `RuntimeError` が発生しても**最大5回までリトライ**を行う処理を実装してください。
- 最終的に、リトライを経て正常に取得された DataFrame を取得し、標準出力に表示してください。

---

## 解答のコードを書いてみてください。

**最低10分は何も見ずに自力で書き切ってから提出してください。**

実務で即戦力となる堅牢なエラーハンドリングの実装コードを待っている！