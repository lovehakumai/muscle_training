直近のセッションで「苦手なところ」として挙げてくれた**「groupbyによる集計（特にグループ内の最大値を持つ行全体の特定）」**ですね。

前回のレビューで解説した `idxmax()` や、複数キーでの制御を実際に手を動かして脳と指先に染み込ませる、絶好の復習ドリルを用意しました。ここを乗り越えれば、実務のデータパイプラインにおけるデータ集計で迷うことはなくなります！

それでは、データエンジニアとしての腕試しです。新しい課題を出題します。

---

# 課題：README.md

今回選んだテーマ: **1. pandasでのグルーピング・集計（各グループにおける最大値を持つレコード全体の抽出）**

### シナリオ:
日次の広告キャンペーン配信ログから、各キャンペーンが「最もインプレッション（表示回数）を獲得した日」のパフォーマンスデータを特定し、その日のクリック数や日付を含む行全体を抽出するデータパイプラインの前処理を実装します。

### 制約条件:
- 各キャンペーン（`campaign_id`）ごとに、インプレッション数（`impressions`）が最大となる日のレコードを1行ずつ抽出すること。
- 前回解説した `idxmax()` を用いたエレガントな方法、または `groupby` + `merge` を用いた堅牢な方法のいずれか（あるいは両方のパターン）で実装してください。

### 準備するもの:
以下のコードをコピーして、君のスクリプトのベースにしてくれ。
コメントアウトは英語でのみ表現すること。

```python
import pandas as pd

# ----------------------------------------------------
# Daily campaign performance log data
# ----------------------------------------------------
df_perf = pd.DataFrame({
    "campaign_id": [101, 101, 101, 102, 102, 103],
    "date": ["2026-09-01", "2026-09-02", "2026-09-03", "2026-09-01", "2026-09-02", "2026-09-01"],
    "impressions": [1500, 3200, 2400, 4500, 4100, 800],
    "clicks": [120, 310, 180, 400, 380, 50]
})
```

### 期待するスクリプトの条件:
1. `df_perf` から、各 `campaign_id` において `impressions` が最大（MAX）となる日のデータ行全体（`campaign_id`, `date`, `impressions`, `clicks`）を抽出する関数を実装してください。
2. 最終的に出力される DataFrame の行数が、ユニークなキャンペーン数（今回は 3 件）と完全に一致することを確認するアサーション（assert）を記述してください。
3. クレンジングおよび抽出完了後の DataFrame を出力してください。

---

## 解答のコードを書いてみてください。

**【絶対のルール】**
**最低10分は何も見ずに自力で書き切ってから提出してください。**
構文を思い出せなくても、前回の解説（`.shape` からの `axis` の覚え方、`idxmax()` の使い方）を脳内で再生しながら、まずは粘り強くコードを叩き出してみてください！

君からの挑戦的なコード（そしてPRコメント）を楽しみに待っています！