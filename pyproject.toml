[project]
name = "muscle-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lovehakumai", email = "masaotomeza@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.3",
    "dbt-snowflake>=1.12.0",
    "polars>=1.43.2",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "seaborn>=0.13.2",
    "tenacity>=9.1.4",
]

[project.scripts]
muscle-training = "muscle_training:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
